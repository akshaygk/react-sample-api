import React from 'react'
import { shallow } from 'enzyme';
import Bookmarked from './index';

// let wrapped = shallow(<Bookmarked/>);
describe('Bookmarked component page', () => {

  it('should render the Bookmarked Component correctly', () => {   
    console.log("Render")
  });
});